//console.log ("Variable js shikbo")


let name ="sabbir dewan";
name="sakib dewan";              // reassigning the value of name variable. i can overwrite the value of name variable because it is declared with let keyword.
console.log(name);

//const age=20;
//age =24;                       //does not allow to reassign the value of age variable, i can't overwrite the value of age variable because it is declared with const keyword.  
//console.log(age);

const isAdult = true;
console.log(typeof(isAdult))

//console.log(typeof (name), typeof (age) , typeof (isAdult), typeof (null), typeof (undefined), typeof ({}), typeof ([]), typeof (function(){}));

//ssbbnnuo= string+symbol+bigint+boolean+number+null+undefined+object







